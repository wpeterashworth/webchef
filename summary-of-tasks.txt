2026-06-23 — Peter
Set up the database models only (no server, client, or dependencies).
Created supabase/migrations/20260623000000_initial_schema.sql defining 6 tables:
  - users (account + XP/streak gamification state, keyed by Auth0 sub)
  - units (top-level course groupings, ordered)
  - lessons (belong to a unit, ordered, XP reward)
  - steps (lesson interactions; type-tagged with JSONB content)
  - user_progress (per-user lesson completion + score, unique per user+lesson)
  - recipes (recipe library, optionally tied to a lesson)
Added FK indexes and CHECK constraints. Leaderboard intentionally left as a
query over users.xp (not a table). RLS policies, functions, and seed data
deferred to separate tasks.

2026-06-23 — Peter
Installed the Supabase CLI as a pinned dev dependency (supabase ^2.107.0) via
npm. Created root package.json + package-lock.json (committed for reproducible
team installs). Run the CLI with `npx supabase`. node_modules already gitignored.
Next: supabase init + login + link + db push (needs project credentials).

2026-06-30 — Josh
Migrated the frontend from a static Svelte entry flow to SvelteKit structure.
Moved component files into client/src/lib/components, global styles into
client/src/app.css, and image assets into client/static/images (favicon moved to
client/static/favicon.ico). Added SvelteKit config and app shell files:
client/svelte.config.js, client/vite.config.js, and client/src/app.html.
Created client/src/routes/+layout.svelte and client/src/routes/+page.svelte to
replace app mounting, and moved lesson.svelte to
client/src/routes/lesson/+page.svelte. Removed legacy entry files
(client/index.html, client/app.svelte, client/scripts/main.js).
Installed @sveltejs/vite-plugin-svelte and verified migration with a successful
`pnpm run build` in client.

2026-07-08 — Peter
Implemented signup / login / logout with Supabase Auth (email + password),
client-side flow. Installed @supabase/supabase-js in client. Added:
  - client/src/lib/supabase/client.js — single browser Supabase client, reads
    PUBLIC_SUPABASE_URL + PUBLIC_SUPABASE_ANON_KEY from client/.env
  - client/src/lib/stores/auth.js — global `user` store (getSession on load +
    onAuthStateChange keep it in sync) and a `logout()` helper; guarded by
    `browser` so it doesn't run during SSR
  - client/src/routes/login/+page.svelte and .../signup/+page.svelte — the two
    routes the header already linked to (were 404ing), styled with app.css color
    variables
Updated client/src/lib/components/header.svelte so the nav reacts to auth state:
signed-out shows Login / Sign Up; signed-in shows the user's email + Logout.
Added client/.env + client/.env.example and a .gitignore exception so the
template stays committed while real .env is ignored. Verified with `pnpm build`
and manual testing of signup/login/logout. Auth chosen over the ADR's Auth0
because the DB is already on Supabase; PROJECT-TASKLIST.txt updated to say
Supabase Auth throughout (agents.md tech-stack table still lists Auth0 — left
untouched per Peter). Next: signed-in route protection (Phase 3.4) and creating
a users-table row on first login (Phase 3.3), pending Peter's go-ahead.

2026-07-09 — Peter
Configured the client for deployment as a free Render Static Site. The app has
no server-side code (all auth/data goes browser-side to Supabase), so a web
server isn't needed. Switched the SvelteKit adapter from @sveltejs/adapter-auto
to @sveltejs/adapter-static (Render isn't in adapter-auto's supported-host list).
Changes:
  - client/svelte.config.js — use adapter-static with fallback: "200.html"
  - client/src/routes/+layout.js (new) — ssr = false + prerender = false, making
    the app a pure SPA (avoids running Supabase client code during a build-time
    SSR pass)
  - render.yaml (new, repo root) — Render Blueprint: static site, rootDir client,
    build `pnpm install && pnpm build`, publish build/, a /* -> /200.html rewrite
    for SPA deep links, and PUBLIC_SUPABASE_URL / PUBLIC_SUPABASE_ANON_KEY as
    dashboard-set (sync: false) env vars
Verified with `pnpm build` — build/ contains 200.html + _app assets. .env was
already gitignored/untracked, so no gitignore change needed. Cost: $0 (Render
Static Site + Supabase free tier). Next: connect the repo in Render and deploy.

2026-07-09 — Peter
Fixed a Render build failure caused by prerendering. The build crashed with
`Error: 404 /lesson/lesson-... (linked from /lesson)` and exit code 1, even
though everything worked on localhost. Root cause: the lesson list page
(client/src/routes/lesson/+page.svelte) links each card to /lesson/<lessonId>,
but no route existed to handle those URLs. Localhost (vite dev) never noticed
because nothing crawls the links; Render runs `vite build`, where the
adapter-static prerender crawler follows every link and treats an unreachable
one as fatal (strict mode). With prerender = true set globally in
client/src/routes/+layout.js, the missing detail route 404'd and killed the
build. Fix — created the dynamic route (new files, no teammate conflicts):
  - client/src/routes/lesson/[lessonId]/+page.js — load() looks up the lesson by
    params.lessonId across the three bundled JSON files (RecipeAdjust, Howtocook,
    IngredientSub) and throws error(404) if not found. Runs at build time, so
    each lesson prerenders to its own HTML file.
  - client/src/routes/lesson/[lessonId]/+page.svelte — lesson detail view:
    intro, learning goal / meta, each question with options (correct one
    highlighted) and a collapsible <details> answer/explanation/safety-tip,
    then the recap. Uses native <details> so it needs no JS and prerenders
    cleanly; styled to match lesson-card.svelte.
Verified with `pnpm build` — build now succeeds and build/lesson/ contains all
12 prerendered lesson HTML files (previously the 12 that were 404ing). Note: the
data was never the problem (it was already bundled/importable) — the route was
simply missing. Left the on:submit deprecation warnings on login/signup alone
(harmless, non-fatal). Next: commit + push these two files so the Render deploy
goes through.

2026-07-10 — Luke
Fixed the lessons page so lesson cards render visibly. Updated
client/src/routes/lesson/+page.svelte with a proper page shell, header, and
responsive grid (12 cards from bundled JSON). Updated lesson-card.svelte to use
Svelte 5 $props(), theme CSS variables (--panel-color, --text-color) for dark/light
visibility, and the wood-frame border (matching landing.svelte) with z-index so
card content stays on top. Moved Lessons nav link in header.svelte so it shows
for all users (not only when signed in). Fixed lesson detail page to use
$derived(data.lesson). Verified with `pnpm build` and dev server at /lesson.

2026-07-10 — Luke
Restructured lessons into 4 top-level cards (Recipe Adjustment, Ingredient
Substitutions, How to Cook, Utensil Handling). Added client/src/lib/lessons.js to
group the 12 JSON skills into 4 category lessons. Lesson list now shows 4 cards
with skill + question counts. Rewrote lesson/[lessonId]/+page.svelte as a
step-by-step flow per skill: intro → interactive quiz (one question at a time with
feedback) → skill recap → next skill. Updated +page.js with entries() to prerender
only the 4 lesson routes. Verified with `pnpm build`.

2026-07-10 — Luke
Added per-card difficulty selection (Beginner, Intermediate, Advanced) on
lesson-card.svelte. Each level controls how many skills and questions are included
(client/src/lib/lessons.js). Start Lesson passes ?difficulty= in the URL; the
lesson flow filters content accordingly and shows the chosen level in the header.
Verified with `pnpm build`.

2026-07-14 — Luke
Implemented the point system from point-system.txt in client code and Supabase.
Added supabase/migrations/20260714220000_point_system.sql: levels table (0–20 with
titles, XP thresholds, unlocks), users.level_number/level_title, user_progress
difficulty/points_earned, complete_lesson RPC (awards 10/25/50 XP by difficulty,
updates level), and get_leaderboard RPC (level 1+). Client: points.js constants,
profile store/API, completeLesson wired from lesson finish flow, difficulty gates
on lesson cards (Intermediate @ 2, Expert @ 5), dashboard level/XP display,
leaderboard page (unlocks @ level 1), header nav link. Verified with `pnpm build`.
Next: team applies migration via `npx supabase db push` before testing end-to-end.
